{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": ["...", "npm run build:web"]
    }
  },
  "deploy": {
    "startCommand": "npm run serve:web",
    "inputs": [{ "step": "build" }]
  }
}
