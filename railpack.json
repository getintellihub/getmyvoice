{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": ["...", "npx expo export -p web"]
    }
  },
  "deploy": {
    "startCommand": "npx serve dist -s -l $PORT",
    "inputs": [{ "step": "build" }]
  }
}
